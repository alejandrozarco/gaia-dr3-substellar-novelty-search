# State-cycler re-rank v2 — 2026-08-13 (RETRY_NEEDED: injection FAILED, v3 fix prescribed)

## Lane gate decision

RETRY_NEEDED — injection failed, so the 0-survivor result is uninterpretable for the target class and the lane can be neither closed nor declared alive. ZTF19abxfaon (the archetype the re-rank was built to rescue) dies at Stage 2 of the v2 chain itself: its per-band p98-p2 corrected amplitude is 1.31 mag (r) because ALL amplitude statistics computed on detections only are capped at the detected bright-state spread (~18.3-20.6 mag); the true 5.2 mag range lives between r~23 quiescence (below alert threshold, zero detections) and outburst. Worse, the g band drops out entirely (hostless => corrected=false => all 235 magpsf_corr null), and the percentile trimming makes v2 STRICTER than v1's flawed difference-space proxy (1.31 vs 2.24) for exactly this class. The v2 metric is validated for detected-state cyclers (13/21 of its passers are catalogued dwarf novae recovered from v1's kill list) but has ~0 completeness for faint-quiescence/hostless cyclers, so the Poisson upper limit diverges when divided by completeness. SPECIFIC FIX for v3: (1) amplitude = brightest magpsf minus the deepest contemporaneous diffmaglim from the ALeRCE non_detections endpoint at the same position (or ZFPS forced photometry), i.e. measure against non-detections, not detection percentiles; (2) never require corrected photometry for hostless objects (fall back to raw magpsf / limit-based amplitude when corrected=false); (3) keep the stage-3 pre-kills unchanged (injection passes both: neg_diff_frac=0.0, bright_ref=False under both variants) and keep the 5as screen (audit showed 0 false wide-radius kills); (4) mandatory ordering: re-run the ZTF19abxfaon injection through the full v3 chain FIRST and require recovery before re-processing the 1,976 candidates; (5) add a transient veto (TNS window overlap) since the v2 passer list already caught one SN masquerading as a cycler.

## Injection test (the decisive result)

ZTF19abxfaon is NOT recovered by the v2 chain — it dies at STAGE 2 (corrected-amplitude), a v2 design bug. Workings saved to /tmp/state_cyclers/rerank_v2/injection_test.json; live ALeRCE data fetched 2026-08-13.

STAGE 1 — v1 harvest cuts: PASS (all). Live listing: ndet=586 (>=30 OK), firstmjd=58732.23 (in [58194,60565] OK), lastmjd=61264.37 (>=58894 OK), span=2532.1 d (>=700 OK), sigmara/dec=0.0012" (<=0.5 OK), and stage-2 magstats DIFFERENCE amp_proxy (min-max, band ndet>=5) = 2.241 >= 2.0 — a marginal graze past the cut, exactly matching the known compression (true 5.2 mag -> 2.24 in alert space).

STAGE 2 — v2 corrected amplitude (exact re-ranker prescription: per-band p98-p2 of magpsf_corr, finite, 5<mag<30, band ndet>=5): FAIL. g-band: magstats fid=1 corrected=false, ALL 235 g detections have magpsf_corr=null -> g drops out entirely (0 usable of 235). r-band: 284/284 corrected, amp_corr_r = 1.313. amp_corr = 1.313 < 2.0 -> pass=False under all three re-ranker scripts (rerank1/2/4 share this gate; rerank2's magstats-corr fallback doesn't trigger since r-band lightcurve values exist, and magmin/max_corr are null anyway). Diagnostic: even raw-diff p98-p2 is only g=1.445 / r=1.421 (percentile trimming alone cuts the marginal 2.24 min-max below 2.0).

STAGE 3 — pre-kills: would PASS both. neg_diff_frac = 0.000 (no kill; all-positive diffs as expected for the archetype). bright_ref = False under BOTH implemented variants (rerank1/4 any-det distnr<1.5"&magnr<17: no such det; rerank2 median variant: no valid distnr/magnr pairs at all — hostless, med_distnr=null).

DESIGN BUG for synthesis: the object's 5.2 mag amplitude lives between r~23 quiescence (below alert threshold -> ZERO detections) and outburst, so ANY detection-only amplitude statistic — corrected or not — is capped at the detected bright-state spread (~18.3–20.6). v2's percentile-robust corrected range (1.31) is actually SMALLER than v1's difference-space min-max proxy (2.24) for this class, and the g-band loses correction entirely (hostless => corrected=false). The amp_corr>=2.0 gate therefore systematically kills faint-quiescence/hostless state-cyclers — the target class the re-rank was built to rescue. Fix direction: compute amplitude against non-detections (brightest magpsf vs deepest contemporaneous diffmaglim at the position) or forced photometry (ZFPS), not detection-only percentile ranges; do not require corrected photometry for hostless objects.

## Conditional density (detected-state sub-class ONLY)

NOT COMPUTABLE for the target class — the injection failure means chain completeness for faint-quiescence/hostless state-cyclers is ~0, so 0 survivors gives an unbounded density limit (3.0/(298.5 deg2 x C) with C≈0). A CONDITIONAL limit valid only for state-cyclers whose full amplitude >=2.0 mag is contained within their DETECTED states (dwarf-nova-like, with usable corrected photometry): 0 novel survivors over 298.5 deg2 -> Poisson 95% UL <3.0 objects -> <0.0101 uncatalogued objects per deg2, and even that assumes ~100% completeness within that sub-class, which was not independently injected.


Conditional sub-class extrapolation only: <0.0101 deg-2 x ~15,000 deg2 usable ZTF footprint ~ <151 uncatalogued detected-state cyclers skywide (95%). Two strong caveats: (a) the strip (RA 330-360, Dec 0..+10) sits at high |b| (~ -40 to -55 deg), and CV/state-cycler surface density rises steeply toward the Galactic plane, so a flat extrapolation underestimates the plane-adjacent population and the number is a footprint-averaged fiction at low |b|; (b) for the actual target class (faint-quiescence, hostless, ZTF19abxfaon-like) NO skywide statement is possible until a v3 chain passes injection.

## Caveats

1) Injection FAILED (verdict.recovered=false in /tmp/state_cyclers/rerank_v2/injection_test.json): the archetype grazes v1's diff-space cut at 2.24 but fails v2's corrected-amplitude gate at 1.31 — a design bug, not a data gap; every density number above is therefore conditional or void. 2) Detection-only amplitude statistics are structurally blind to below-threshold quiescence; fixing requires non-detection limits or forced photometry, which the v3 retry must use. 3) Hostless objects lose their g band entirely under corrected photometry (magpsf_corr=null when corrected=false) — 5 objects in harvest_2 were fully uncorrectable and auto-failed. 4) The 19 passers were 100% catalogued (17 local xmatch <1.8as, 1 SIMBAD, 1 TNS SN Ia), consistent with the metric working for detected-state dwarf novae; but one passer (ZTF19aaagfme) was a supernova whose light curve produced amp_corr=2.38 — SN contamination inflates any naive pass count and a TNS-window veto is not yet a chain stage. 5) The premise that v1's screen used a 30as kill radius was WRONG: all 274 v1 kills were sub-2as matches, re-tested clean at 5as, 0 resurrections — so no candidates were lost to wide-radius false kills, but this also means the v1 null was not artificially strict there. 6) The conditional <0.0101 deg-2 assumes ~100% completeness for the detected-state sub-class, which was never separately injection-tested. 7) Skywide extrapolation ignores the |b| gradient; the strip is high-latitude and unrepresentative of the plane. 8) neg_diff_frac>0.5 pre-killed 207/418 in harvest_3 alone — correct for bright-ref inflation, but any genuine cycler observed mostly BELOW its reference level would also be killed; not tested by the injection (archetype is all-positive). 9) No verification deep-dives were run (0 survivors reached that stage), so the verification tier of the chain remains unexercised in v2. 10) All conclusions rest on ALeRCE lightcurve/magstats snapshots fetched 2026-08-13; ndet and lastmjd will drift.

## Stats

rerank pass: [{"n_in": 571, "n_pass": 5}, {"n_in": 418, "n_pass": 5}, {"n_in": 317, "n_pass": 1}] (one substrip agent returned empty — v3 reprocesses all 1,976 regardless); screen: 19 in, 19 catalogued (13 known dwarf novae recovered from v1 kills, 1 SN), 0 survivors.